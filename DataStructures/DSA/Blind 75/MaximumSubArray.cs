using System;
using System.Collections.Generic;

namespace DSA.Blind_75
{
    // Find a contigous sub array whose sum is largest and return sum
    internal class MaximumSubArray
    {
        public static void Main(string[] args)
        {
            BruteForceMethod();


        }

        private static void BruteForceMethod()
        {
            int[] ipArray = [2, -1, 3, 4, -1, 2, 1, -5, 4];
            int maxSum = 0;
            List<int> opList = new List<int>();

            for (int i = 0; i < ipArray.Length; i++)
            {

            }
        }
    }
}
